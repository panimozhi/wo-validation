DROP TABLE currency;

-- Create table variable
CREATE TABLE currency (
  name   VARCHAR(20),
  code   VARCHAR(3),
  symbol VARCHAR(5)
);

--ALTER TABLE currency CONVERT TO CHARACTER SET utf8;

